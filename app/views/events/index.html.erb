<h1>Events</h1>

<table>
  <thead>
    <tr>
      <td>Location</td>
      <td>Date/Time</td>
    </tr>
  </thead>
  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= event.location %></td>
        <td><%= event.occurs_at %></td>
      </tr>
    <% end %>
  </tbody>
</table>